function love.conf(t)
    t.window.title = "Pang"  -- Replace with your game's title
    t.window.width = 800                -- Optional: set window width
    t.window.height = 600               -- Optional: set window height
    -- t.console = true                 -- Uncomment if you want to see the console (useful for debugging)
end